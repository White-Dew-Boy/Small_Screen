# 项目规则 —— 不要自动构建

本文件会被每个新会话自动加载，作为本项目（ESP32-S3 / ESP-IDF + LVGL 固件）的常驻规则：

- **不要自动运行任何构建 / 烧录 / 编译命令**，包括但不限于：
  - `idf.py build`、`idf.py flash`、`idf.py monitor`、`idf.py erase-flash`
  - `cmake --build`、`ninja` 等直接构建命令
  - 任何会占用串口（COM5）或修改 `build/` 产物的命令
- 原因：构建/烧录由用户在自己的工具链环境中自行执行；沙箱环境与用户的 ESP-IDF 环境不一致（如 venv 路径、工具链注册），自动构建容易失败或干扰用户。
- 代码改动完成后，如果按惯例需要编译验证，**停下来告知用户"代码已就绪，请自行构建"**，不要替用户启动构建。
- 唯一例外：用户在当前会话中**明确要求**执行某条构建/烧录命令。
